document.addEventListener('DOMContentLoaded', function() {
    // A lógica de carregar produtos foi movida para o backend (PHP).
    console.log("Página carregada. A listagem de produtos é controlada pelo PHP.");
});
